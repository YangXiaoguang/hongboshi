# 运营管理后台建设路线图

## 目标

红博士后续会逐步成为一个带有课程商品、咨询服务、用户权益、订单交易、支付退款、财务核算和风险复核的心理服务交易平台。运营管理后台不是简单的页面集合，而是承载业务状态机、权限、审计、对账和人工处理流程的稳定系统。

本路线图用于指导 Codex 持续二次开发：每个阶段都要能独立交付、可测试、可回滚，并且为后续阶段留下清晰的扩展点。

## 当前基础

已完成或具备基础能力：

- 前端使用 React、TypeScript、Vite、Tailwind CSS。
- 后端使用 Express 模块化 API。
- `shared/domain` 已承载前后端共享 Zod 契约。
- 已建立 `visitor`、`member`、`counselor`、`catalog_viewer`、`catalog_operator`、`operator`、`admin` 角色和 `admin:manage`、`counseling:fulfill`、`catalog:*`、`membership_product:*`、`user:read`、`user:membership`、`order:read`、`order:operate`、`transaction:read`、`transaction:operate`、`finance:read`、`finance:manage`、`risk:read`、`risk:review`、`risk:sop`、`audit:read` 等权限能力。
- 咨询运营配置页面 `/admin/counseling` 已可管理咨询师档案与资质服务状态、咨询排班、服务记录与履约异常、配置取消规则并查看履约审计。
- 支付对账页面 `/admin/payments` 已可对比支付回调收据、业务订单和咨询预约状态。
- 课程商品列表 `/admin/courses` 已接入统一后台，可读取课程商品契约、搜索、状态筛选、分类筛选、排序、分页、基础信息编辑、详情内容编辑、成交图文素材维护、素材资产登记与合规队列、内容质量校验、审核动作、上下架、改价、资源级权限和最近审计；状态与详情内容均可写入 JSON Store 或 PostgreSQL，并已联动前台课程列表/详情。
- 会员商品后台 `/admin/memberships` 已接入统一后台，可读取会员商品配置、维护商品文案、调整套餐价格、暂停/恢复套餐，并写入会员商品操作审计；当前支持开发期 JSON Store，用户端会员公共快照和服务端会员 checkout 已复用同一 Store，前台已能区分可买、暂不可购买和历史待支付继续支付。
- 用户会员后台 `/admin/users` 已接入统一后台，可按关键词、角色和会员状态筛选用户，聚合账号、会员、课程权益、订单、咨询预约和风险摘要，并支持会员开通、延期、到期标记、计划调整和审计追溯。
- 订单管理后台 `/admin/orders` 已接入统一后台，可按关键词、订单状态和商品类型筛选课程、会员与咨询订单，并展示订单详情、支付回调摘要、关联履约对象、状态时间线、待支付订单关闭、异常标记和操作审计。
- 交易退款后台 `/admin/transactions` 已接入统一后台，可按关键词、流水类型、渠道、处理状态、业务类型和日期范围筛选支付/退款流水，并展示关联订单、业务对象、异常提示和处理时间线；已支持受控退款申请、交易异常工单和操作审计。
- 财务管理后台 `/admin/finance` 已接入统一后台，可按关键词、渠道、业务类型和日期范围查看收入、退款、净收款、退款中金额、异常金额、渠道/业务类型分布、财务口径和脱敏明细，可按当前筛选条件导出 CSV，并可查看/维护自然月账期与渠道手续费规则、结算预览。
- 风险复核台 `/admin/risk` 已接入统一后台，可按风险等级、状态、来源和关键词查看风险事件队列，查看隐私最小化详情、关联对象摘要、服务端 SOP 模板、升级队列和人工处理记录，并执行开始复核、已联系、建议咨询、升级和解决动作；复核记录、SOP 模板和升级队列已支持 PostgreSQL。
- 审计中心 `/admin/audit` 已接入统一后台，可按模块、动作、操作者、资源关键词和日期范围只读聚合课程商品、会员、订单、交易、咨询运营和风险复核既有审计事实，并支持按当前筛选条件导出 CSV 与查看单条事件来源详情；统一审计 Store 已完成方案、归档事件契约、只追加归档表草案、Archive Store 和手动归档 API。
- 咨询预约、支付回调、风险事件、风险复核记录、风险 SOP/升级队列、测评结果、课程权益等模块已开始使用 Store 接口，并具备 PostgreSQL 实现或迁移基础。

主要缺口：

- 课程商品已具备成交图文 URL 资产管理、素材资产登记和合规队列基础，但仍缺少真实素材文件上传/对象存储、签名下载 URL 和学习资料下载履约，内容运营闭环还需要继续收口。
- 会员商品后台已具备配置化、公共快照同步、可用性体验和售卖状态回归，用户端 `/membership`、课程/成长空间会员 checkout 摘要和服务端会员 checkout 已读取同一会员商品 Store；后续需要继续建设会员待支付订单过期状态机、真实支付渠道金额校验和历史待支付订单的受控关闭动作。
- 订单后台已具备首批受控写动作，交易后台已具备退款申请、退款渠道受理摘要、异常工单动作和 PostgreSQL 持久化；财务只读口径、CSV 导出、账期和手续费规则基础已建立，咨询师档案、服务状态、服务记录与履约异常基础已建立，结算批次、渠道结算单、发票和真实支付渠道适配还需要继续建设。
- 风险复核台已形成 SOP 模板、升级队列和 PostgreSQL 持久化基础，但通知协作还需要继续建设。
- 跨模块审计中心已形成只读聚合、导出、详情追踪、统一归档表草案和手动归档试点，但归档后台入口、读取切换、定时归档和长期留存策略还需要继续建设。
- 订单、财务、风控等后台权限仍需按课程商品样板继续拆分到资源级和动作级。

## 建设原则

- 共享契约优先：跨前后端的数据结构先进入 `shared/domain`，再由 client 和 server 消费。
- 服务端掌控状态：订单、支付、退款、咨询预约、财务和风险流转由 server service 决策，前端只提交意图。
- Store 抽象稳定：每个核心领域先定义 Store 接口，再提供内存/JSON/PostgreSQL 实现，API 不直接绑定数据库细节。
- 权限和审计内建：后台读写接口必须校验角色/权限；敏感操作必须记录操作者、对象、动作、前后状态和时间。
- 页面只做呈现：筛选、排序、状态推导、异常判断优先放在 feature repository 或 server service。
- 小步可交付：每个阶段都要有测试、文档更新、CI 验证、Git 提交和推送。

## 分阶段计划

| 阶段  | 名称             | 状态   | 核心产出                                                 |
| ----- | ---------------- | ------ | -------------------------------------------------------- |
| M0    | 连续执行基建     | 已完成 | 后台路线图、Codex 执行状态、操作协议                     |
| M1    | 统一后台框架     | 已完成 | `/admin` shell、导航、权限守卫、现有后台页面接入         |
| M2    | 课程商品管理     | 已完成 | 课程商品列表、上下架、价格、发布联动、数据库化           |
| UX-Z  | 会员商品配置     | 已完成 | 会员商品、套餐价格、套餐状态生命周期和审计               |
| UX-AA | 会员前台快照同步 | 已完成 | 用户端会员快照、动态结算金额、暂停套餐新单拒绝           |
| UX-AB | 会员可用性体验   | 已完成 | 不可售提示、历史订单金额、前台快照预览和运营链接提示     |
| UX-AC | 会员售卖回归     | 已完成 | 改价/暂停/恢复售卖回归、不可售 reason 和支付前策略       |
| M3    | 用户与会员管理   | 已完成 | 用户检索、画像摘要、会员权益、协议与状态                 |
| M4    | 订单管理         | 已完成 | 订单列表、详情、状态机、课程/咨询订单聚合                |
| M5    | 交易与退款管理   | 已完成 | 支付流水、退款申请、异常处理、渠道适配预留               |
| M6    | 财务管理         | 已完成 | 收入统计、退款统计、净收款、异常金额、导出和规则         |
| M7    | 咨询运营增强     | 已完成 | 咨询师档案、排班、服务记录、履约异常、规则版本           |
| M8    | 风险复核台       | 已完成 | 高风险复核、处理记录、SOP 模板、升级队列和持久化         |
| M9    | 审计中心         | 进行中 | 只读聚合、追踪、导出和手动归档试点已完成，归档入口待建设 |

## M1: 统一后台框架

目标：把已存在的 `/admin/counseling` 和 `/admin/payments` 纳入统一后台体验，为后续课程、用户、订单、财务模块建立稳定骨架。

交付内容：

- 新增 `client/src/pages/admin/AdminLayout.tsx`，提供后台侧边导航、顶部上下文、权限提示和内容区域。
- 新增 `client/src/pages/admin/AdminHome.tsx`，展示后台模块入口、关键风险提示和当前建设状态。
- 新增后台导航配置，统一声明模块 key、标题、路径、权限和状态。
- 保留现有 URL：`/admin/counseling`、`/admin/payments` 继续可访问，但使用统一布局。
- `AppHeader` 中的运营入口指向 `/admin`，再由后台内导航进入具体模块。
- 增加前端测试覆盖后台鉴权、导航和现有页面接入。
- 更新 README、产品工程路线和本执行状态文件。

验收标准：

- 未登录用户访问 `/admin` 能看到登录提示。
- 非运营/管理员用户访问 `/admin` 能看到权限不足提示。
- `operator` 或 `admin` 用户可进入后台首页，并能导航到咨询运营和支付对账。
- 现有咨询运营、支付对账页面功能不回退。
- `pnpm run ci` 通过。

## M2: 课程商品管理

目标：把课程从展示型 seed 数据升级为可运营的课程商品。

交付内容：

- `shared/domain/courseProduct.ts` 定义课程商品、分类、价格、状态、审核状态和列表查询契约。
- `server/modules/catalog` 建立课程商品 Store 和后台 API，支持从课程 seed 初始化商品快照，并可用 JSON Store 或 PostgreSQL 持久化。
- 后台课程商品列表 `/admin/courses` 支持搜索、分类、状态筛选、排序和分页。
- 课程商品支持基础信息编辑、详情内容编辑、成交图文素材维护、素材资产登记与合规处理、审核动作、价格编辑、上下架动作和审计记录。
- 前台课程读取已从课程商品 Store 读取 `published + approved` 商品，并同步价格和会员权益。
- 下一步进入课程素材文件上传/对象存储和学习资料下载履约，用户与会员管理主链路已完成首轮后台化。

验收标准：

- 运营可在后台看到课程商品清单并执行上下架。
- 前台只展示已审核通过且已上架课程。
- 价格和状态变更有审计记录。

当前状态：

- M2-A 已完成：共享契约、只读 API、前端 repository、后台列表和导航接入。
- M2-B 已完成：新增服务端写动作、价格编辑、上下架规则和课程商品审计事件。
- M2-C 已完成：课程商品 JSON Store、前台发布状态过滤和价格联动。
- M2-D 已完成：课程商品 PostgreSQL Store、基础信息编辑、数据库迁移和审计事件。
- M2-E 已完成：课程商品审核状态流、审核动作 API、驳回原因展示和详情内容契约。
- M2-F 已完成：课程详情内容 JSON Store、后台内容编辑、前台详情内容读取和复审保护。
- M2-G 已完成：课程详情内容 PostgreSQL Store、批量内容校验、审核提交前校验和后台校验状态。
- M2-H 已完成：课程商品权限细化与素材字段预留。
- M2-I 已完成：课程详情成交图文素材契约、后台编辑、前台详情消费和 `sales_assets` PostgreSQL 字段。
- M2-J 已完成：课程素材资产共享契约、JSON Store、后台读取/登记/合规处理 API 和后台素材队列入口。

## M3: 用户与会员管理

目标：建立客服和运营可用的用户管理入口，同时保护心理服务隐私边界。

交付内容：

- 用户列表、搜索、角色/会员状态筛选。
- 用户详情聚合基础资料、会员权益、课程权益、订单摘要、咨询预约摘要和风险提示，M3-A 已完成只读视图。
- 敏感字段最小化展示，并为查看敏感信息预留更高权限。
- 会员开通、到期、权益调整走服务端动作和审计。

验收标准：

- 运营可以按手机号、昵称、用户 ID 检索用户。
- 用户详情不直接暴露咨询记录全文和测评答案。
- 会员权益调整可追溯。

当前状态：

- M3-A 已完成：`user:read` 权限、用户后台聚合契约、`/api/users/admin/users` 列表/详情、开发期 fallback 用户、前端 `/admin/users` 只读台和隐私最小化详情。
- M3-B 已完成：`user:membership` 权限、会员开通/延期/到期标记/计划调整契约、会员操作 API、会员操作审计 Store、PostgreSQL 审计表、前端会员动作入口和审计列表。
- M3 已完成，下一步围绕订单状态动作、交易退款和财务口径继续展开。

## M4: 订单管理

目标：把课程订单、咨询预约订单和会员订单纳入统一订单管理。

交付内容：

- 统一订单领域契约，区分商品类型、金额、优惠、支付状态、履约状态和退款状态。
- 后台订单列表、详情、筛选和状态时间线。
- 订单状态机动作：关闭待支付、标记异常、解除异常、查看支付/退款关联。
- 课程权益、咨询预约和会员权益都通过订单状态驱动。

验收标准：

- 订单详情可以解释当前状态来自哪个支付或业务事件。
- 订单状态变更不会绕过服务端状态机。
- 关键动作有权限校验和审计。

当前状态：

- M4-A 已完成：`order:read` 权限、订单后台列表/详情契约、`/api/orders/admin/orders` 列表/详情、课程权益订单/咨询预约/支付回调收据聚合、前端 `/admin/orders` 只读台和后台导航接入。
- M4-B 已完成：`order:operate` 权限、订单动作契约、待支付订单关闭、异常标记/解除、JSON/PostgreSQL 订单操作审计和前端订单详情动作入口。
- M4 已完成，M5-A 交易流水只读台、M5-B 退款申请/异常工单动作与 M5-C 交易操作数据库化/渠道适配接口已完成，M6-A 财务只读台、M6-B 财务导出和 M6-C 账期手续费规则已完成，下一步进入咨询运营增强。

## M5: 交易与退款管理

目标：建立支付流水、退款流水和异常处理入口，为真实支付渠道接入打基础。

交付内容：

- 支付渠道适配接口，支持微信、支付宝或模拟渠道逐步替换。
- 交易流水列表和详情，关联订单、回调收据和业务对象。
- 退款申请、退款回调、退款异常处理动作。
- 支付对账异常从只读提示升级为可处理工单。

验收标准：

- 每笔交易都能追溯到订单和业务对象。
- 重复回调、金额不一致、业务状态不一致能够被标记并处理。
- 退款动作不会直接修改业务状态，必须通过回调或受控模拟事件完成闭环。

当前状态：

- M5-A 已完成：`transaction:read` 权限、交易后台列表/详情契约、`/api/transactions/admin/transactions` 列表/详情、支付/退款回调收据、课程/会员/咨询订单、业务对象和订单异常标记聚合，以及前端 `/admin/transactions` 只读台。
- M5-B 已完成：`transaction:operate` 权限、退款申请、交易异常工单、交易操作审计、内存/JSON 交易操作 Store 和前端交易详情动作入口；退款申请只进入 `refunding`，完成态仍由 `refund.succeeded` 回调驱动。
- M5-C 已完成：建立交易操作 PostgreSQL Store、交易后台迁移表、退款渠道适配接口和受理摘要审计；渠道拒绝或失败不会修改订单，受理成功也不直接绕过回调修改业务完成态。

## M6: 财务管理

目标：让运营和财务可以查看收入、退款、实收、导出、账期和手续费基础数据。

交付内容：

- 财务看板：收入、退款、净收入、退款中金额、待处理异常。
- 按日期、商品类型、支付渠道聚合统计，M6-A 已完成只读聚合。
- CSV 导出基础能力，M6-B 已完成生成时间、筛选条件、汇总金额、口径版本和稳定字段。
- 财务口径文档化：下单金额、支付金额、退款金额、实收金额、自然月账期和手续费规则。

验收标准：

- 财务统计来自交易/订单领域，不从页面临时计算。
- 导出字段稳定，并带有生成时间和筛选条件。
- 账期和手续费规则带有版本、生效时间和渠道维度，结算预览来自服务端统一口径。
- 数据口径在文档中明确。

当前状态：

- M6-A 已完成：`finance:read` 权限、财务后台共享契约、`/api/finance/admin/overview` 只读聚合、`/admin/finance` 财务页面、后台导航接入和财务口径说明。
- M6-B 已完成：`FinanceAdminExportSchema`、`/api/finance/admin/export`、CSV 文件元数据、筛选条件快照、导出字段稳定性、前端导出入口和账期/手续费/结算/发票预留字段。
- M6-C 已完成：`FinanceAdminRuleConfigSchema`、`FinanceRuleStore`、`/api/finance/admin/rules`、`finance:manage` 权限、自然月账期、渠道费率/固定手续费/最低手续费和服务端结算预览。
- M7-A 已完成：`CounselingAdminScheduleConsoleSchema`、`/api/counseling/admin/schedules`、`/admin/counseling` 排班管理区和排班动作审计，运营可维护可预约时段且不能覆盖锁定/已预约时段。
- M7-B 已完成：`CounselingServiceRecordConsoleSchema`、`/api/counseling/admin/service-records`、`/admin/counseling` 服务记录与履约异常区，运营可查看咨询师/状态/异常筛选、异常摘要和隐私最小化服务记录。
- M7-C 已完成：`CounselorAdminProfileConsoleSchema`、`CounselorAdminProfileStore`、`/api/counseling/admin/counselors` 和 `/admin/counseling` 咨询师档案与服务状态区，运营可维护展示资料、接单开关、资质摘要和资质状态；暂停接单、资质待复核或资质过期的咨询师不会进入前台可预约列表。

## M7: 咨询运营增强

目标：把咨询业务从预约闭环升级到可运营的服务管理。

交付内容：

- 咨询师排班管理。M7-A 已完成基础版：未来 30 天排班、服务状态、新增/关闭/恢复时段和冲突保护。
- 咨询师资质和服务状态管理。M7-C 已完成基础版：展示资料、擅长方向、价格、资质摘要、资质状态、资质到期时间、接单开关和服务状态。
- 履约异常、未到访、改期、取消原因统计。M7-B 已完成基础版：待支付锁定临近/过期、退款中、取消待退款、未到访、临近开始未确认等服务端异常聚合。
- 取消规则版本化和灰度配置预留。

验收标准：

- 咨询师服务能力可以被运营配置，当前已支持可预约时段维护。
- 咨询师档案、资质摘要和接单状态来自服务端 Store，当前已通过后台接口维护并写入咨询运营审计。
- 排班变更和规则变更可追溯，当前排班动作进入咨询运营审计。
- 已有关联预约的时段不能被危险修改，当前服务端会拦截锁定/已预约时段关闭或覆盖。
- 服务记录来自服务端聚合，当前不会向运营后台暴露咨询说明、测评答案或风险信号原文。

## M8: 风险复核台

目标：承接心理测评和咨询前信息中的高风险信号。

交付内容：

- 风险事件列表、详情、来源和等级。M8-A 已完成基础版：队列、筛选、分页、脱敏用户摘要、关联对象摘要和隐私提示。
- 人工复核动作：开始复核、标记已联系、建议咨询、升级处理、关闭。M8-A 已完成服务端状态机、处理备注校验和处理记录。
- 风险 SOP 模板和处理结果模板。M8-B 已完成基础版：服务端按风险等级/来源匹配 SOP 模板，处理记录保存模板 ID/版本和结果模板 ID。
- 升级队列。M8-B 已完成基础版：`escalate` 动作保存升级原因、优先级、负责人/待分配状态，并在 `/admin/risk` 展示队列摘要。
- 风险复核持久化。M8-C 已完成：风险复核记录、SOP 模板和升级队列已建立 PostgreSQL Store，并为 M9 审计中心预备 actor/resource/action/before/after 投影字段。
- 与用户详情、咨询预约、测评报告建立受控关联。

验收标准：

- 高风险事件不会只停留在日志里，当前已进入 `/admin/risk` 工作台。
- 复核动作有处理人、处理时间和结果，当前已写入 `RiskReviewStore`，并能记录 SOP 模板版本、升级摘要和审计中心投影字段。
- 敏感内容按权限展示，当前默认不输出测评答案、咨询前说明全文或风险信号原文。

## M9: 审计中心

目标：建立跨模块操作追踪能力。

交付内容：

- 统一审计事件契约。M9-A 已完成 `AuditCenterListResultSchema` 与 `AuditCenterEventSchema`，先以只读聚合方式复用既有审计事实，不新增写入真相源。
- 后台审计列表支持按操作者、对象、模块、动作、时间筛选。M9-A 已完成 `/api/audit/admin/events` 和 `/admin/audit`。
- 关键模块逐步接入统一审计。M9-A 已接入课程商品、会员、订单、交易、咨询运营和风险复核既有审计事实。
- 导出和事件详情基础能力。M9-B 已完成 `AuditCenterExportSchema`、`AuditCenterDetailResultSchema`、`/api/audit/admin/export` 和 `/api/audit/admin/events/:eventId`，复用同一套筛选、权限和隐私边界。
- 统一审计 Store 方案预研与只追加归档边界。M9-C 已完成 `AuditCenterArchiveEventSchema`、`docs/audit-store-architecture.md`、`audit_center_archived_events` 归档表草案和 schema 测试，明确保留现有业务 Store 真相源，只用归档表做长期检索和回填投影。
- 审计归档任务与只读校验试点。M9-D 已完成 `AuditArchiveStore`、PostgreSQL Archive Store、`POST /api/audit/admin/archive`、`audit:archive` 权限、归档映射、幂等写入、失败摘要和隐私裁剪测试，不做业务双写和读取切换。
- 归档后台入口与归档只读校验。M9-E 已完成 `/admin/audit` 管理员可见的归档触发/批次结果区、`GET /api/audit/admin/archive/verification`、归档/校验前端仓储和权限测试，默认仍不切换主列表数据源。
- 归档表只读检索预览。M9-F 已完成 `AuditCenterArchiveSearch*` 契约、Archive Store 长期检索筛选、`GET /api/audit/admin/archive/events`、前端归档预览仓储和 `/admin/audit` 管理员预览区，权限继续使用 `audit:archive`，默认仍不切换主列表数据源。
- 下一步 M9-G 建议做审计归档读取策略预研：设计按时间窗口读取切换、留存告警、迁移回滚和长期归档巡检方案，先形成可维护设计和小范围开关，不直接替换全量主列表。

验收标准：

- 敏感后台动作都能在审计中心追踪。
- 审计事件不可被普通后台操作修改。
- 审计查询性能有基础索引设计；当前归档表已包含模块、动作、资源、操作者、来源和归档时间索引，手动归档任务、只读校验和归档预览可验证幂等写入、回填覆盖、长期检索和归档差异。

## 每阶段固定交付清单

- 更新或新增 `shared/domain` 契约。
- 增加 server service、API、Store 接口和必要 PostgreSQL 迁移。
- 增加 frontend repository、页面、状态和错误处理。
- 增加权限校验、审计事件和异常提示。
- 增加单元测试或集成测试。
- 更新相关 docs 和 `docs/codex-execution-state.md`。
- 运行 `pnpm run ci`。
- 提交 Git commit 并推送到 GitHub。
